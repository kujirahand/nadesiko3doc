●『[[ローカルストレージキー削除:plugin_browser/ローカルストレージキー削除]]』の詳しい解説

ブラウザのローカルストレージのキー(KEY)を削除。

▲簡単な利用例
保存やローカルストレージ保存のサンプルを試してからどうぞ。
- [[plugin_browser/保存]]
- [[plugin_browser/ローカルストレージ保存]]
{{{#nako3
ローカルストレージキー列挙を言う。
「ユーザー名」をローカルストレージキー削除。
ローカルストレージキー列挙を言う。
「飲み物」をローカルストレージキー削除。
ローカルストレージキー列挙を言う。
}}}

▲ローカルストレージキーの一覧を表示し、選択して削除するサンプル。
{{{#nako3(rows=10)
「ローカルストレージキーの削除」のラベル作成。改行作成。
LSキー＝ローカルストレージキー列挙。
LSキーのセレクトボックス作成し、LSキーセレクトに代入。
LSキーセレクトの「multiple」にオンをDOM属性設定。
もし、(LSキーの要素数)＜15ならば、
　LSキーセレクトの「size」に(LSキーの要素数)をDOM属性設定。
違えば、
　LSキーセレクトの「size」に「15」をDOM属性設定。
ここまで。
「削除」のボタン作成し、削除ボタンに代入。

削除ボタンをクリックした時には
　削除キー＝空配列。
　（LSキーセレクトの要素数）回
　　　LSキーセレクト[回数-1]の「selected」をDOM属性取得。
　　　もし、それ＝はいならば、削除キーに（LSキーセレクト[回数-1]の「value」をDOM属性取得）を配列追加。
　ここまで。
　　削除キー一覧＝削除キーの「,」を改行に置換
　　「削除していい？{改行}{改行}{削除キー一覧}」で二択。
　　もし、それがはいならば、
　　　削除キーを反復
　　　　対象をローカルストレージキー削除。
　　　　I＝LSキーから対象を配列検索。
　　　　LSキーのIを配列切取り。
　　　　LSキーセレクトにDOM親要素設定。
　　　　LSキーセレクト[I]をDOM子要素削除。
　　　ここまで。
　　ここまで。
ここまで。
}}}

