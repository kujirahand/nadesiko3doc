▲説明

POSTパラメータ($_POST)から、キーKEYの値を取得して返します。値が存在しない(または空)場合は、省略値DEFを返します。

助詞は「KEYを」「DEFで」の順で指定します。値が見つからない場合に備えて省略値を必ず指定できるため、直接[[plugin_php/POST]]を参照するよりも安全に使えます。この命令はなでしこ3PHP(phpnako)専用です。

▲利用例

{{{
"name"を「名無し」でPOST取得して表示。
}}}

▲参考

- [[plugin_php/POST]]
- [[plugin_php/GET取得]]
