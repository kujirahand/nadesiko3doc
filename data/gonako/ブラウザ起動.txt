●説明

既定のブラウザ（または既定のアプリケーション）でURL・パスSを開きます。OSごとに実際のコマンドを切り替えて起動します（macOSは`open`、Windowsは`start`、その他は`xdg-open`）。起動は非同期に行われ、戻り値はありません。

●利用例

{{{#nako3
「https://example.com/」をブラウザ起動
}}}

このサンプルは実際にブラウザを起動してしまうため、DocTestでは実行結果を検証していません。

●参照

- [[gonako/起動]]
