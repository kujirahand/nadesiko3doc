●説明

パスSのフォルダを作成します。中間の親フォルダが存在しない場合はまとめて再帰的に作成します（既に存在していてもエラーにはなりません）。

●利用例

{{{#nako3
TMP=一時フォルダ作成
(TMP&"/sub")にフォルダ作成
「存在:{(TMP&"/sub")がフォルダ存在}」と表示
### GO表示結果: 存在:true
}}}

●参照

- [[gonako/フォルダ存在]]
