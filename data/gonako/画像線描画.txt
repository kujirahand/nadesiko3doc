●説明

アクティブな画像キャンバスに対して直線を描画します（画面への描画ではなく、メモリ上の画像データに対する変更です）。描画結果を画像ファイルとして保存するには『画像保存』命令を使います。

座標は`[開始X, 開始Y, 終了X, 終了Y]`の配列（ピクセル単位）で指定し、色は色名（「赤」「青」など）、`#RRGGBB`、`#RRGGBBAA`のいずれかの形式の文字列で指定します。

●利用例

{{{#nako3
[50,50]の画像新規作成をIDに代入。
[0,0,49,49]を「緑」で画像線描画。
// 描画結果のピクセル色を確認
[10,10]の画像点取得を表示。
画像閉。
### GO表示結果: #008000FF
}}}

※ 画像ファイルとして書き出す例:
```
[100, 100]の画像新規作成。
「白」に画像背景色設定。
[0, 0, 99, 99]を「赤」で画像線描画。
"line.png"へ画像保存。
画像閉。
```

●参照

- [[gonako/画像新規作成]]
- [[gonako/画像保存]]
- [[gonako/画像矩形描画]]
- [[gonako/画像円描画]]
