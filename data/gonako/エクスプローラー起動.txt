●説明

指定したパスSを、OS標準のファイラーで開きます。Windowsではエクスプローラー、macOSではFinder、Linuxでは既定のファイルマネージャで開かれます（実装上は「ブラウザ起動」命令と同じ処理で、`open`/`start`/`xdg-open`コマンドを呼び出します）。実際に外部プロセス（ファイラー）を起動する命令のため、このマニュアルでは実行結果を検証していません。

●利用例

{{{#nako3
「{カレントディレクトリ取得}」をエクスプローラー起動。
}}}

●参照

- [[gonako/カレントディレクトリ取得]]
