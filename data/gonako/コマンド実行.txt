●説明

シェルコマンドSを新しいプロセスとして起動し、終了を待たずに次の行へ進みます。実装上は「起動」命令と同じ命令で、Windowsでは`cmd /c`、それ以外のOSでは`sh -c`経由でコマンドを実行します。実際に外部プロセスを起動する命令のため、このマニュアルでは実行結果を検証していません。

●利用例

{{{#nako3
「open .」をコマンド実行。
}}}

●参照

- [[gonako/コマンド実行待機]]
