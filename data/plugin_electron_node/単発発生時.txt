▲説明

対象にて、指定のイベントが発生した時の処理を登録します(`obj.once(種類, callback)`)。『[[plugin_electron_node/発生時]]』と異なり、登録した処理はイベント発生時に1回だけ実行され、その後は自動的に削除されます。

助詞は「callbackで」「対象の」「種類」の順で指定します。

▲利用例

{{{
窓の「ready-to-show」単発発生時には(イベント)
　窓をウインドウ表示。
ここまで
}}}

▲参考

- [[plugin_electron_node/発生時]]
