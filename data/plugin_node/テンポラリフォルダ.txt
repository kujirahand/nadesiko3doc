▲説明

OSのテンポラリフォルダ(一時ファイル用フォルダ)のパスを取得して返します。Node.js専用の命令です。内部では `os.tmpdir()` を利用しています。

戻り値の内容はOSや環境によって異なります(例: macOSでは `/var/folders/...`、Windowsでは `C:\Users\ユーザー名\AppData\Local\Temp` など)。

▲利用例

{{{
# テンポラリフォルダのパスを取得する（実行例。結果は環境により異なる）
テンポラリフォルダを表示。
}}}

▲参考

- [[plugin_node/一時フォルダ作成]]
