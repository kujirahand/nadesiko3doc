▲説明

暗号強度の強い乱数を使って、指定した個数分のバイト配列(Uint8Array、各要素は0～255の整数)を生成して返します。内部ではNode.jsの`crypto.getRandomValues()`を利用しています。

実行するたびに異なる値が返るため、DocTestのように結果を固定して確認することはできません。

▲利用例

{{{
# ランダムな16バイトの配列を生成する（実行例。実行するたびに結果が変わる）
配列=16のランダム配列生成。
配列を表示。
}}}

▲参考

- [[plugin_node/ランダムUUID生成]]
